spring:
  application:
    name: Mine4Cut-server

  datasource:
    # H2 데이터베이스
    #url: jdbc:h2:mem:testdb
    #driver-class-name: org.h2.Driver
    #username: sa
    #password:

    # PostgreSQL
    url: jdbc:postgresql://localhost:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: password

  #h2:
    #console:
      #enabled: true
      #path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show_sql: true
    Properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: PoR5ahCTbqB2MPlUtqq6nksLWgI/M1eVk19ZoVOppRY=
  expiration: 86400 // 초 단위

cors:
  allowed-origins: "http://localhost:3000,https://Mine4Cut.com"